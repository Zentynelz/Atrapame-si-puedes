<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tron_dark_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_logo_cyber"
            android:contentDescription="@string/app_name" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/main_title"
            style="@style/TronTextTitle"
            android:textSize="32sp"
            android:layout_marginBottom="8dp"
            android:gravity="center"
            android:shadowColor="@color/tron_cyan_glow"
            android:shadowRadius="10"
            android:shadowDx="0"
            android:shadowDy="0" />

        <View
            android:layout_width="200dp"
            android:layout_height="2dp"
            android:background="@color/tron_cyan"
            android:layout_marginBottom="48dp" />

        <Button
            android:id="@+id/btnPlay"
            style="@style/GameButtonPrimary"
            android:text="@string/btn_play"
            android:drawableStart="@android:drawable/ic_media_play"
            android:drawableTint="@color/tron_dark_bg"
            android:drawablePadding="8dp" />

        <Button
            android:id="@+id/btnConfig"
            style="@style/GameButton"
            android:text="@string/btn_config"
            android:drawableStart="@android:drawable/ic_menu_preferences"
            android:drawableTint="@color/tron_cyan"
            android:drawablePadding="8dp" />

        <Button
            android:id="@+id/btnScores"
            style="@style/GameButton"
            android:text="Puntuaciones Globales"
            android:drawableStart="@android:drawable/ic_menu_sort_by_size"
            android:drawableTint="@color/tron_cyan"
            android:drawablePadding="8dp" />

        <Button
            android:id="@+id/btnMyStats"
            style="@style/GameButton"
            android:text="Mis Estadísticas"
            android:drawableStart="@android:drawable/ic_menu_myplaces"
            android:drawableTint="@color/tron_cyan"
            android:drawablePadding="8dp" />

        <Button
            android:id="@+id/btnExit"
            style="@style/GameButton"
            android:text="@string/btn_exit"
            android:strokeColor="@color/tron_orange"
            android:textColor="@color/tron_orange"
            android:drawableStart="@android:drawable/ic_menu_close_clear_cancel"
            android:drawableTint="@color/tron_orange"
            android:drawablePadding="8dp"
            android:layout_marginBottom="24dp" />

        <!-- Debug Stress Meter Panel (Admin View) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@drawable/tron_panel_bg"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Stress Meter (Debug):"
                style="@style/TronTextStats"
                android:textColor="@color/tron_orange"
                android:layout_marginEnd="12dp" />

            <ProgressBar
                android:id="@+id/progressMainStress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:max="100"
                android:progress="0"
                android:progressTint="@color/tron_orange" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>